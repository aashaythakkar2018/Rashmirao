<footer>
  <div class="ft-grid">
    <div>
      <div class="ft-brand">
        {%- if section.settings.logo != blank -%}
          <img class="ft-brand-mark" src="{{ section.settings.logo | image_url: width: 302 }}"
               alt="" aria-hidden="true" width="151" height="168" loading="lazy" decoding="async">
        {%- endif -%}
        <span class="ft-brand-type">{{ section.settings.wordmark | default: shop.name }}</span>
      </div>
      <p class="ft-tagline">{{ section.settings.tagline }}</p>

      {%- assign has_social = false -%}
      {%- if settings.social_instagram_link != blank or settings.social_facebook_link != blank or settings.social_pinterest_link != blank or settings.social_tiktok_link != blank or settings.social_youtube_link != blank -%}
        {%- assign has_social = true -%}
      {%- endif -%}
      {%- if has_social -%}
        <div class="ft-social">
          {%- if settings.social_instagram_link != blank -%}
            <a href="{{ settings.social_instagram_link }}" target="_blank" rel="noopener" aria-label="Instagram">{% render 'icon-social', platform: 'instagram' %}</a>
          {%- endif -%}
          {%- if settings.social_facebook_link != blank -%}
            <a href="{{ settings.social_facebook_link }}" target="_blank" rel="noopener" aria-label="Facebook">{% render 'icon-social', platform: 'facebook' %}</a>
          {%- endif -%}
          {%- if settings.social_pinterest_link != blank -%}
            <a href="{{ settings.social_pinterest_link }}" target="_blank" rel="noopener" aria-label="Pinterest">{% render 'icon-social', platform: 'pinterest' %}</a>
          {%- endif -%}
          {%- if settings.social_tiktok_link != blank -%}
            <a href="{{ settings.social_tiktok_link }}" target="_blank" rel="noopener" aria-label="TikTok">{% render 'icon-social', platform: 'tiktok' %}</a>
          {%- endif -%}
          {%- if settings.social_youtube_link != blank -%}
            <a href="{{ settings.social_youtube_link }}" target="_blank" rel="noopener" aria-label="YouTube">{% render 'icon-social', platform: 'youtube' %}</a>
          {%- endif -%}
        </div>
      {%- endif -%}

      {%- if shop.enabled_payment_types.size > 0 -%}
        <div class="ft-payments">
          {%- for type in shop.enabled_payment_types -%}
            {{ type | payment_type_svg_tag }}
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>

    {%- for block in section.blocks -%}
      {%- if block.type == 'menu' -%}
        <div {{ block.shopify_attributes }}>
          <p class="ft-hd">{{ block.settings.heading }}</p>
          <ul class="ft-links">
            {%- for link in block.settings.menu.links -%}
              <li><a href="{{ link.url }}">{{ link.title }}</a></li>
            {%- endfor -%}
          </ul>
        </div>
      {%- endif -%}
    {%- endfor -%}
  </div>

  <div class="ft-bottom">
    <span>&copy; {{ 'now' | date: '%Y' }} {{ shop.name }}. {{ 'general.footer.rights' | t }}</span>
    <span>
      {%- for policy in shop.policies -%}
        {%- if policy != blank -%}
          <a href="{{ policy.url }}">{{ policy.title }}</a>{%- unless forloop.last %} &nbsp;&middot;&nbsp; {% endunless -%}
        {%- endif -%}
      {%- endfor -%}
    </span>
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo mark (light, for the dark footer)" },
    { "type": "text", "id": "wordmark", "label": "Wordmark", "default": "Rhytara" },
    { "type": "textarea", "id": "tagline", "label": "Tagline",
      "default": "Contemporary paintings, wearable art, and limited editions. Every work is an original. Signed, certified, made to last." }
  ],
  "blocks": [
    {
      "type": "menu",
      "name": "Link column",
      "settings": [
        { "type": "text", "id": "heading", "label": "Heading", "default": "Collections" },
        { "type": "link_list", "id": "menu", "label": "Menu" }
      ]
    }
  ],
  "max_blocks": 4,
  "default": {
    "blocks": [
      { "type": "menu", "settings": { "heading": "Collections" } },
      { "type": "menu", "settings": { "heading": "Studio" } },
      { "type": "menu", "settings": { "heading": "Services" } }
    ]
  }
}
{% endschema %}
