Place one representative image per design here, named after the design
`code` in config/designs.json, e.g. `EOE.jpg`, `SG.jpg`, `PPR.jpg`, `GN.jpg`,
`MP.jpg`. `templateData.ts` looks up `assets/artwork/{code}.jpg` for the
certificate's artwork panel; if the file is missing the panel is hidden
rather than broken.
