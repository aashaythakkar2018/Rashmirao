Place the Rhytara logo here, e.g. `rhytara-logo.png` (transparent background,
~600px wide is plenty for a 20mm-tall print slot). Then set its path/URL in
`src/services/certificate/templateData.ts` (`logo_url`).
